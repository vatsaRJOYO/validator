package com.vatsa.validator;

public enum ReturnType {
    /**
     * COUNTER: the type of the object returned is an object of class Counter
     */
    COUNTER,

    /**
     * BOOLEAN: the type of the object returned is an of object of class Boolean or boolean
     * i.e. A predicate function
     */
    BOOLEAN;
}
